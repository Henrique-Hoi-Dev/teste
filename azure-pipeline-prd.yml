---
name: '1.0.0-rc.3783'

trigger:
  branches:
    include: ["main"]

pool:
 name: Mirae

resources:
  repositories:
    - repository: templates
      type: git
      name: "Portal Publico/devops-templates"
      ref: refs/heads/portal-publico

stages:
- template: build/docker.yaml@templates
  parameters:
    image: 'portal-publico'
// const interval = useRef<ReturnType<typeof setTimeout> | null>(null);

// useEffect(() => {
//   if (!interval.current)
//     interval.current = setInterval(() => {
//       setNum((prevState) => prevState + 1);
//     }, 10);

//   if (num === 1000) clearInterval(interval.current);
// }, [num]);
